*Help*
`/hangbot start {word} {hint}` _Start a game with the given word and sets a hint if given_
`/hangbot guess {character}` _Guess a single letter_
`/hangbot guess {word}` _Guess the word_
`/hangbot hint {hint}` _Change the hint (Gamemaster only)_
`/hangbot abort` _Abort the game (Gamemaster only)_